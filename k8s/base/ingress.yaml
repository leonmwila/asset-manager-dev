apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: oemis-odoo
spec:
  rules:
    - host: oemis.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: oemis-odoo
                port:
                  number: 8069
