<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- Repair Order PDF Report -->
    <record id="action_report_repair_order" model="ir.actions.report">
        <field name="name">Repair Order</field>
        <field name="model">repair.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">company_extension.report_repair_order_document</field>
        <field name="report_file">company_extension.report_repair_order_document</field>
        <field name="binding_model_id" ref="repair.model_repair_order"/>
        <field name="binding_type">report</field>
        <field name="print_report_name">'Repair Order - %s' % (object.name)</field>
    </record>

    <!-- Payment Receipt Report -->
    <record id="action_report_repair_receipt" model="ir.actions.report">
        <field name="name">Payment Receipt</field>
        <field name="model">repair.order</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">company_extension.report_repair_receipt_document</field>
        <field name="report_file">company_extension.report_repair_receipt_document</field>
        <field name="binding_model_id" ref="repair.model_repair_order"/>
        <field name="binding_type">report</field>
        <field name="print_report_name">'Receipt - %s' % (object.name)</field>
    </record>
</odoo>
